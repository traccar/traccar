#set($subject = "$esc.html($device.name): Durum Bilinmiyor")
#set($digest = "$esc.html($device.name) Durum Bilinmiyor $dateTool.format('yyyy-MM-dd HH:mm:ss', $event.eventTime, $locale, $timezone)")
<!DOCTYPE html>
<html>
<body>
Cihaz: $esc.html($device.name)<br>
Durum Bilinmiyor<br>
Zaman: $dateTool.format('yyyy-MM-dd HH:mm:ss', $event.eventTime, $locale, $timezone)<br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Bildirim Aboneliğini İptal Et</a>
</body>
</html>
