#set($subject = "$esc.html($device.name): μετακίνηση χωρίς συνοδεία")
#set($digest = "$device.name μετακίνηση χωρίς συνοδεία στο $dateTool.format('yyyy-MM-dd HH:mm:ss', $event.eventTime, $locale, $timezone)")
<!DOCTYPE html>
<html>
<body>
Συσκευή: $esc.html($device.name)<br>
Μετακίνηση χωρίς συνοδεία<br>
Ώρα: $dateTool.format('yyyy-MM-dd HH:mm:ss', $event.eventTime, $locale, $timezone)<br>
Σημείο: <a href="$webUrl/event/$event.id">#{if}($position.address)$position.address#{else}$position.latitude&deg;, $position.longitude&deg;#{end}</a><br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Απεγγραφή</a>
</body>
</html>
