#set($subject = "$esc.html($device.name): rifornimento carburante")
#set($digest = "$esc.html($device.name) rifornimento carburante alle $dateTool.format('dd/MM/yyyy HH:mm:ss', $event.eventTime, $locale, $timezone)")
<!DOCTYPE html>
<html>
<body>
Dispositivo: $esc.html($device.name)<br>
Evento: rifornimento carburante<br>
Orario: $dateTool.format('dd/MM/yyyy HH:mm:ss', $event.eventTime, $locale, $timezone)<br>
Quantità prima: $esc.html($event.attributes.before)<br>
Quantità dopo: $esc.html($event.attributes.after)<br>
Posizione: <a href="$webUrl/event/$event.id">#{if}($position.address)$position.address#{else}$position.latitude&deg;, $position.longitude&deg;#{end}</a><br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Annulla iscrizione</a>
</body>
</html>
