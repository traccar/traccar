#set($subject = "$device.name: Kraftstoffzunahme")
#set($digest = "$device.name Kraftstoffzunahme am $dateTool.format('dd.MM.yyyy', $event.eventTime, $locale, $timezone) um $dateTool.format('HH:mm:ss', $event.eventTime, $locale, $timezone) Uhr")
<!DOCTYPE html>
<html>
<body>
Ger&auml;t: $device.name<br>
am $dateTool.format('dd.MM.yyyy', $event.eventTime, $locale, $timezone) um $dateTool.format('HH:mm:ss', $event.eventTime, $locale, $timezone) Uhr<br>
Vorheriger Wert: $event.attributes.before<br>
Aktueller Wert: $event.attributes.after<br>
Position: <a href="$webUrl/event/$event.id">#{if}($position.address)$position.address#{else}$position.latitude&deg;, $position.longitude&deg;#{end}</a><br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Unsubscribe</a>
</body>
</html>
