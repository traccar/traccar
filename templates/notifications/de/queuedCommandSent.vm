#set($subject = "$esc.html($device.name): Befehl in Warteschlange gesendet")
#set($digest = "Warteschlangenbefehl an $esc.html($device.name) am $dateTool.format('dd.MM.yyyy', $event.eventTime, $locale, $timezone) um $dateTool.format('HH:mm:ss', $event.eventTime, $locale, $timezone) Uhr gesendet")
<!DOCTYPE html>
<html>
<body>
Ger&auml;t: $esc.html($device.name)<br>
Befehl in Warteschlange gesendet<br>
am $dateTool.format('dd.MM.yyyy', $event.eventTime, $locale, $timezone) um $dateTool.format('HH:mm:ss', $event.eventTime, $locale, $timezone) Uhr gesendet<br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Unsubscribe</a>
</body>
</html>
