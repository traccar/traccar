#set($subject = "Device expiration reminder")
#set($digest = "Device $esc.html($device.name) expires on $dateTool.format('yyyy-MM-dd', $expiration, $locale, $timezone)")
<!DOCTYPE html>
<html>
<body>
Your device $esc.html($device.name) will expire on $dateTool.format('yyyy-MM-dd', $expiration, $locale, $timezone).
</body>
</html>
